import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  padding: 60px;
  text-align: center;
  font-size: 1.8rem;
`;

const FreeBoardPage = () => {
  return <Wrapper>📝 자유 게시판입니다 (준비 중)</Wrapper>;
};

export default FreeBoardPage;
